class Image {

  constructor({id, url, name, like_count, comments, image_id}){
    this.id = id
    this.url = url
    this.name = name
    this.like_count = like_count
    this.comments = comments
    this.image_id = image_id
  }

}
